<div class="container p-4">
    <div class="row">
        <div class="col-md-12 mx-auto">
            <div class="card text-center">
                <div class="card-body">
                    {{!-- myUser es variable global declarada en index.js -> app.locals.myUser = req.user; --}}
                    <h3>Welcome {{myUser.fullname}}</h3>
                    <h4>{{myUser.username}}</h4>
                    <a href="/links" class="btn btn-primary m-4">Save your links</a>
                </div>
            </div>
        </div>
    </div>
</div>